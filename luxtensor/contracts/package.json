{
    "name": "mdt-contracts",
    "version": "1.0.0",
    "description": "ModernTensor ERC20 Token Contracts",
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "deploy:testnet": "npx hardhat run scripts/deploy.js --network luxtensor_testnet",
        "deploy:mainnet": "npx hardhat run scripts/deploy.js --network luxtensor_mainnet",
        "execute-tge": "npx hardhat run scripts/execute-tge.js --network luxtensor_testnet",
        "verify": "npx hardhat verify"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.1"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.4"
    }
}
